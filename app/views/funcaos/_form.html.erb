<%= form_for @funcao, :html => { :class => "form-horizontal funcao" } do |f| %>

  <% if @funcao.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@funcao.errors.count, "error") %> prohibited this funcao from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @funcao.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :cargo, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :cargo, :class => 'form-control' %>
    </div>
    <%=f.error_span(:cargo) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                funcaos_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
